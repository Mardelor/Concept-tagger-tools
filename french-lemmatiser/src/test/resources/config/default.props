# annotators
annotators = tokenize, ssplit, custom.lemma

# tokenize
tokenize.language = fr

# pos
pos.model = edu/stanford/nlp/models/pos-tagger/french/french-ud.tagger

# custom.lemma
customAnnotatorClass.custom.lemma = fr.insee.stamina.nlp.FrenchLemmaAnnotator
